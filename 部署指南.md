# ShowWhen 博客部署指南

技术栈：[Astro](https://astro.build/) + [Fuwari](https://github.com/saicaca/fuwari) 主题，托管在 GitHub Pages，域名 **showwhen.top**。

推送代码到 GitHub 的 `main` 分支后，GitHub Actions 会自动构建并发布，无需本地打包上传。

---

## 一、本地运行（可选）

需要 Node.js 20+（推荐 22）与 pnpm 9：

```shell
npm install -g pnpm@9
pnpm install
pnpm dev        # 本地预览，浏览器打开 http://localhost:4321
pnpm build      # 构建，产物在 dist/
```

## 二、写文章

```shell
pnpm new-post 我的文章标题
```

在 `src/content/posts/` 下编辑生成的 Markdown 文件：

```yaml
---
title: 文章标题
published: 2026-09-25
description: 摘要，会显示在列表和 SEO 中
tags: ["标签1", "标签2"]
category: 分类
draft: false        # true 为草稿，不发布
---
```

支持代码高亮、数学公式（KaTeX）、提示框（`:::tip` / `:::note` / `:::warning`）、图片灯箱、目录、阅读时长等。

## 三、推送到 GitHub

### 方式 A：GitHub CLI（最快）

安装 <https://cli.github.com/> 后登录：

```shell
gh auth login
```

在项目目录执行（把 `你的用户名` 换掉，仓库名随意，例如 `blog`）：

```shell
gh repo create blog --public --source=. --remote=origin --push
```

### 方式 B：手动建仓库

1. 在 GitHub 新建一个 **Public** 仓库（名字随意，例如 `blog`），不要勾选 README。
2. 在项目目录执行：

```shell
git init -b main
git add .
git commit -m "init: ShowWhen blog"
git remote add origin https://github.com/你的用户名/blog.git
git push -u origin main
```

## 四、开启 GitHub Pages

仓库页面 → **Settings → Pages**：

- **Build and deployment → Source** 选择 **GitHub Actions**（不要选 Deploy from branch）。

推送后，在 **Actions** 标签页能看到 “Deploy to GitHub Pages” 工作流，约 1-2 分钟完成。此时 `https://你的用户名.github.io/blog/` 已可访问。

## 五、配置域名 showwhen.top（关键）

到你买域名的服务商控制台（阿里云 / 腾讯云 / Cloudflare 等）添加解析记录：

| 记录类型 | 主机记录 | 记录值 |
|---|---|---|
| A | `@` | `185.199.108.153` |
| A | `@` | `185.199.109.153` |
| A | `@` | `185.199.110.153` |
| A | `@` | `185.199.111.153` |
| CNAME | `www` | `你的用户名.github.io.` |

> 项目里的 `public/CNAME` 已写好 `showwhen.top`，部署后 GitHub 会自动识别自定义域名。

然后回到 **Settings → Pages → Custom domain**，填入 `showwhen.top`，保存，并勾选 **Enforce HTTPS**（证书需等待几分钟到几十分钟，勾选框变可点后再勾）。

DNS 生效后访问 <https://showwhen.top> 即可。

## 六、个性化修改清单

| 想改什么 | 改哪里 |
|---|---|
| 博客标题 / 副标题 / 语言 | `src/config.ts` 的 `siteConfig` |
| 昵称 / 简介 / 头像 | `src/config.ts` 的 `profileConfig`，头像替换 `src/assets/images/demo-avatar.png` |
| 社交链接（GitHub、邮箱等） | `src/config.ts` 的 `profileConfig.links`，图标查 <https://icones.js.org/> |
| 导航栏菜单 | `src/config.ts` 的 `navBarConfig` |
| 主题强调色 | `src/config.ts` 的 `themeColor.hue`（0 红 / 200 青绿 / 250 蓝紫 / 345 粉） |
| 顶部横幅图 | `src/config.ts` 的 `banner` 改为 `enable: true` 并放图 |
| 关于页内容 | `src/content/spec/about.md` |
| 文章版权协议 | `src/config.ts` 的 `licenseConfig` |

改完 `git add . && git commit -m "update" && git push`，网站自动更新。
